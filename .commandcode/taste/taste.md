# nix
- Organize home-manager packages in separate files by category under a `packages/` subdirectory rather than a single monolithic file. Confidence: 0.60
- Use HTTPS clone URLs (not SSH) in README installation instructions since git may not be configured for SSH keys during first-time setup. Confidence: 0.72
- Use `with pkgs;` in package list files instead of repeating the `pkgs.` prefix on every attribute. Confidence: 0.75

# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

